# План исправлений на основе QWEN_sniper.md + QWEN_sniper2.md

## 🔴 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ (безопасность)

### 1. Усилить `sanitize_postgrest()` — защита от PostgREST инъекций
**Файл:** [`app/utils.py`](app/utils.py:217)
**Проблема:** Функция удаляет `&` и экранирует `*` и `.`, но пропускает `(`, `)`, `,`, `"`, `'`. Злоумышленник может сделать:
- `city=eq.Moscow,or(1,eq,1)` — вернуть всю таблицу
- `city=in.(Moscow,Paris)` — инъекция оператора `in`
**Исправление:** Добавить удаление `(`, `)`, `,`, `"`, `'`

### 2. Создать `test_sanitize.py` — тесты PostgREST инъекций
Проверить что инъекционные строки не проходят через `sanitize_postgrest()` и не вызывают 500 ошибок

### 3. Создать `test_rate_limit.py` — тест rate limiting
10 POST запросов → 11-й должен быть заблокирован (flash message)

## 🟡 ВАЖНЫЕ ПРОВЕРКИ

### 4. Проверить Double-Click защиту в JS
- Кнопка «Опубликовать» на `/job/<id>/publish` должна disabled после клика
- Кнопка «Откликнуться» должна disabled после клика
- Кнопки массовых операций должны блокироваться

### 5. Проверить Tailwind — не используется ли CDN
- `tailwind.min.css` (33 KB) должен грузиться локально
- `cdn.tailwindcss.com` НЕ должен загружаться

### 6. Проверить `contact_paid` поля в схеме
- Убедиться что старые поля не вызывают ошибок при создании откликов

## 📦 НОВЫЕ ТЕСТЫ

| Файл | Описание |
|------|----------|
| `test_sanitize.py` | PostgREST инъекции через поиск |
| `test_rate_limit.py` | 11 запросов → блокировка |

## ⏭️ ОТЛОЖЕНО (не реализуем сейчас)

| Пункт | Причина |
|-------|--------|
| Полный E2E для Playwright | Требует миграции с Selenium |
| PWA/TWA assetlinks проверка | Уже работает |
| Tailwind CDN замена | Если обнаружен — простой фикс |
| Часовые пояса | Требует сложной настройки |
| Supabase downtime симуляция | Требует мокирования сети |
| Удаление аккаунта cascade delete | Требует тестового пользователя |
