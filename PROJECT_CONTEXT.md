# Трудник – контекст проекта

## Краткое описание
Веб-приложение для быстрого поиска временной подработки в религиозных организациях (храмы, церкви, мечети и т.д.).
Работодатели (настоятели, администраторы) публикуют задания, а зарегистрированные трудники находят разовую работу.

## Технологический стек
- **Backend**: Python 3.14 + Flask
- **База данных**: Supabase (PostgreSQL) – REST API + Auth + Storage
- **Фронтенд**: HTML5 + Tailwind CSS (CDN) + Jinja2
- **Карты**: Яндекс.Карты (JavaScript API)
- **Хостинг**: PythonAnywhere (бесплатный, стабильный из РФ)
- **CI/CD**: Git + GitHub + ручной `git pull` на PythonAnywhere
- **Версионирование**: Git (коммиты + просмотр версии через иконку 🤝)

## Структура проекта
- **`app.py`** – основной файл приложения (маршруты, декораторы, работа с Supabase)
- **`config.py`** – загрузка конфигурации из `.env`
- **`templates/`** – HTML-шаблоны (Jinja2)
  - `base.html` – базовый шаблон с шапкой и нижним меню
  - `index.html` – лента заданий
  - `workers.html` – поиск работников
  - `my_jobs.html` – мои задания (работодатель)
  - `my_applications.html` – отклики (работодатель)
  - `job_detail.html` – страница задания
  - `profile.html` – страница профиля
  - `register.html` / `login.html` – регистрация / вход
  - и др.
- **`static/`** – CSS, JS, иконки

## Роли пользователей
- **Работник** – ищет задания, откликается, управляет сменами
- **Работодатель** – создаёт задания, управляет откликами, оценивает трудников
- **Админ** – одобряет верификацию работодателей

## Основные функции (реализованы)
- Регистрация и вход через Supabase Auth
- Верификация работодателей (загрузка документа, одобрение админом)
- Создание заданий с фото и картой (Яндекс)
- Лента заданий с фильтрами (город, оплата, расстояние, сортировка)
- Отклики, массовые операции, отзыв откликов
- Смены (чек-ин, завершение, подтверждение оплаты)
- Чат между работником и работодателем (привязан к смене)
- Избранное (люди + задания)
- Чёрный список
- Профиль с фото, навыками, вероисповеданием, портфолио
- Автообновление JWT-токенов
- Отображение версии приложения через Git

## База данных (Supabase)
Основные таблицы:
- **`profiles`** – пользователи (роль, навыки, вероисповедание, рейтинг)
- **`jobs`** – задания
- **`applications`** – отклики
- **`shifts`** – смены
- **`messages`** – сообщения чата
- **`favorites`**, **`blacklists`**, **`reviews`** – избранное, ЧС, отзывы
- **`job_favorites`** – избранные задания

## Текущие задачи (приоритет)
1. Улучшить карточки откликов (краткая информация о труднике, рейтинг, кнопка «Написать», клик → профиль)
2. Массовые операции над откликами (принять/отклонить несколько)
3. Отзыв принятого работника (если до смены >12 часов)
4. Кнопка «Написать» на странице задания и в сменах
5. Обмен файлами в чате