repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        # Defense in depth: never scan MCP/IDE local configs or their templates.
        exclude: 'mcp\.json(\.example)?$'
