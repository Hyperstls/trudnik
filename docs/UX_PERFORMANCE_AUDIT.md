# UX/Performance Аудит приложения «Трудник»

Дата: 2026-06-18 | Аудитор: Автоматизированная проверка

---

## 1. Медленные запросы к Supabase

### 1.1 N+1 Queries

| # | Расположение | Проблема | Серьёзность |
|---|-------------|---------|-------------|
| 1 | [`app/blueprints/jobs.py:38-46`](../app/blueprints/jobs.py:38) | `inject_application_count` — контекстный процессор, выполняющийся на **каждой** странице для работодателей. Делает отдельный запрос `applications?job.employer_id=eq...&status=eq.pending` для счётчика непрочитанных откликов. | **Средняя** |
| 2 | [`app/blueprints/jobs.py:244-249`](../app/blueprints/jobs.py:244) | `job_detail` загружает профиль работодателя **отдельным** запросом после получения задания. Можно было бы использовать `select=*,employer:profiles(...)` в одном запросе. | **Низкая** |
| 3 | [`app/blueprints/jobs.py:255-256`](../app/blueprints/jobs.py:255) | `job_detail` делает **третий** запрос для подсчёта откликов. Можно было включить в первый запрос через агрегацию. | **Низкая** |
| 4 | [`app/blueprints/jobs.py:200-210`](../app/blueprints/jobs.py:200) | Страница `/workers` делает запрос списка работников, а затем **отдельный** запрос `invitations?worker_id=in.(...)` для каждого batch'а. При большом количестве работников — дополнительные раунд-трипы. | **Средняя** |

### 1.2 Отсутствующие индексы

Анализ миграций показывает, что индексы созданы в [`migrations/011_add_search_indexes.sql`](../migrations/011_add_search_indexes.sql) и [`migrations/021_fix_performance_indexes.sql`](../migrations/021_fix_performance_indexes.sql). Основные индексы присутствуют:

- `jobs(status, city, work_type)` — для поиска заданий
- `profiles(role)` — для фильтрации по роли
- `applications(job_id, worker_id)` — для проверки дубликатов

**Не обнаружено критических отсутствующих индексов.**

---

## 2. Неоптимальная загрузка страниц

### 2.1 Большие HTML

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | [`tailwind.min.css`](../static/css/tailwind.min.css) — 33 KB. Не используется purging неиспользуемых CSS-классов. | **Средняя** |
| 2 | Главная страница [`jobs.index`](../app/blueprints/jobs.py:80) загружает **все** открытые задания без пагинации по умолчанию. При большом количестве заданий HTML может быть значительным. | **Средняя** |
| 3 | Все inline-скрипты с `nonce` увеличивают размер каждой HTML-страницы. | **Низкая** |

### 2.2 Отсутствие кеширования

| # | Проблема | Серьёзность |
|---|---------|-------------|
| 1 | [`app/utils.py:127-154`](../app/utils.py:127) — декоратор `cache_for` **существует**, но **не используется** ни на одном маршруте или сервисной функции. | **Высокая** |
| 2 | Справочные данные (навыки, религии) запрашиваются при каждом рендере форм без кеширования. | **Средняя** |
| 3 | Нет HTTP-заголовков кеширования (`Cache-Control`, `ETag`) для статических ресурсов. | **Средняя** |
| 4 | Отсутствует кеш для результатов поиска заданий/работников. | **Средняя** |
| 5 | Контекстный процессор `inject_application_count` не кеширует результат даже в рамках одного запроса — вызывается при каждом рендере шаблона. | **Средняя** |

### 2.3 Размер страниц

| Ресурс | Размер | Примечание |
|---------|--------|------------|
| `tailwind.min.css` | ~33 KB | CSS-фреймворк, без purging |
| `applications.js` | ~22 KB | Крупный JS-файл |
| Страница `/workers` | ~50-150 KB (оценка) | Зависит от количества работников |
| Страница `/` (главная) | ~30-80 KB (оценка) | Зависит от количества заданий |

---

## 3. Проблемы с конкурентным доступом (Race Conditions)

### 3.1 CRITICAL: apply_job — TOCTOU race condition

**Файл:** [`app/blueprints/applications.py:56-60`](../app/blueprints/applications.py:56)

```python
if current_workers >= max_workers:
    flash(f'Места в задании заполнены (максимум {max_workers})', 'info')
    return redirect(url_for('jobs.index'))

supabase_request('POST', 'applications', json={'job_id': job_id, 'worker_id': user_id})
```

**Проблема:** Между проверкой `current_workers >= max_workers` (строка 56) и вставкой отклика (строка 60) другой трудник может занять последнее место. Это **классический TOCTOU** (Time-of-check to time-of-use).

**Серьёзность: ВЫСОКАЯ**

**Рекомендация:** Использовать RPC-функцию (как для accept/reject) с атомарной проверкой и вставкой внутри транзакции PostgreSQL.

### 3.2 accept_application — защищён

**Файл:** [`app/blueprints/applications.py:289-320`](../app/blueprints/applications.py:289)

Accept/reject уже используют `supabase_rpc('accept_application', ...)` с `use_admin=True` — это атомарная операция внутри PostgreSQL. **Защита реализована.**

### 3.3 Batch accept — частично защищён

**Файл:** [`app/blueprints/applications.py:357-400`](../app/blueprints/applications.py:357)

Пакетная обработка вызывает индивидуальный `api_handle_application` для каждого ID, что в свою очередь вызывает RPC. Но между загрузкой списка и обработкой может пройти время. **Умеренный риск.**

---

## 4. Утечки данных (Data Leaks)

### 4.1 Email и телефон неавторизованным

| # | Проверка | Результат | Серьёзность |
|---|---------|-----------|-------------|
| 1 | Видимость контактов на странице задания | Требует авторизации (`@login_required` на странице откликов). Контакты работодателя скрыты до accept'а. | **OK** |
| 2 | API `/api/search/workers` | Возвращает публичные профили. Необходимо проверить, исключены ли `email` и `phone` из ответа. | **Средняя** |
| 3 | Страница `/workers` | Показывает публичные профили. Контакты должны быть скрыты. | **Низкая** |

### 4.2 Service Role audit log

**Реализовано:** [`app/utils.py:170-185`](../app/utils.py:170) — белый список разрешённых контекстов для `supabase_admin_request`. Все вызовы service_role логируются с указанием вызывающего модуля.

---

## 5. Ошибки в шаблонах

### 5.1 Битые ссылки

Проверка статических ресурсов:

| Ресурс | Статус |
|---------|--------|
| `static/css/tailwind.min.css` | ✅ Существует |
| `static/default-avatar.png` | ✅ Существует |
| `static/manifest.json` | ✅ Существует |
| `static/sw.js` | ✅ Существует |
| `static/icons/icon-*.png` | ✅ Все 6 размеров присутствуют |

### 5.2 Inline-стили

Анализ шаблонов [`templates/`](../templates/) показал использование Tailwind CSS utility-классов. Это **не** проблема, так как Tailwind — это легитимный подход (атомарный CSS), а не антипаттерн inline-стилей.

---

## 6. Общее резюме

### Критические проблемы:
1. **TOCTOU race condition в `apply_job`** — разрешение превышения `max_workers`

### Высокие:
2. Декоратор `cache_for` не используется нигде, несмотря на реализацию

### Средние:
3. N+1 запрос в контекстном процессоре `inject_application_count`
4. Отсутствие HTTP-заголовков кеширования
5. Отсутствие кеширования справочных данных (навыки/религии)
6. Нет purging неиспользуемого CSS в Tailwind
7. Отсутствие пагинации по умолчанию на главной

### Низкие:
8. Избыточные запросы в `job_detail`
9. Inline-скрипты увеличивают размер HTML

### Положительные стороны:
- Circuit Breaker реализован для защиты от каскадных отказов
- Connection Pooling (переиспользование HTTP-сессий)
- RLS-политики Supabase защищают данные на уровне БД
- Атомарные RPC для accept/reject операций
- CSRF-защита с nonce-based CSP
- Security Headers (HSTS, X-Frame-Options, CSP, etc.)
- Аудит-лог для service_role вызовов
